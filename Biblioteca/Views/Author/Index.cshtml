@model IEnumerable<Biblioteca.ViewModel.AuthorListingViewModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>


<a asp-action="AddAuthor" data-toggle="modal" data-target="#ModelEditar" class="btn btn-primary edit-radicado"> Create New</a>
<div class="modal fade" id="ModelEditar" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog mod" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title text-center" id="myModalLabel"> Editar Persona</h4>
            </div>
            <div id="edit-person-container"></div>
        </div>
    </div>
</div>
@*<script>
    $(document).on("click", ".editar ", function () {
            var id = $(this).attr("id").split("_")[1];
            var url = "@Url.Action("Edit", "Estadistica")/" + id;
            $("#modalEditar").load(url, function () {
                $("#PersonaId").val(id);
            });
        });
</script>*@
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TotalBook)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TotalBook)
                    </td>
                    <td>
                        <a asp-action="EditAuthor" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
